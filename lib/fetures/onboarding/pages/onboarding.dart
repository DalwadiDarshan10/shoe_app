import 'package:flutter/material.dart';

class OnboardindPage extends StatelessWidget {
  const OnboardindPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Onboarding Page',),
      ),
    );
  }
}